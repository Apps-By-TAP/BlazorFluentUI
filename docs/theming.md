# Theme documentation has moved

The canonical, source-verified reference is the [OKF Theme component page](OKF/components/theme.md).

It documents provider lifetimes, light/dark/custom palette creation, dark-palette detection, built-in values, and all 141 generated CSS custom properties. This legacy path is retained only for existing links.
