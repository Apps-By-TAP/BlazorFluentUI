# Component documentation has moved

The canonical, source-verified reference is the [OKF component index](OKF/components/).

Every compiled Razor component has a dedicated page containing its exact parameters, binding partners, generic arguments, examples, dependencies, accessibility behavior, and source-observable gotchas. This legacy path is retained only for existing links.
